---
name: openclaw-usecase-monitor
description: >
  从 GitHub Repo 读取每日自动抓取的 OpenClaw 相关 use case 数据，生成结论先行的
  中文每日分析报告。覆盖 Reddit、GitHub、X、Discord 四个平台。触发条件：用户说
  "跑今天的 OpenClaw 报告"、"生成 use case 日报"、"OpenClaw 每日监控"、
  "今日 OpenClaw 动态"等。也适用于手动喂入数据后要求生成分析报告的场景。
  当用户提到 OpenClaw、龙虾、use case 监控、每日报告等关键词时，优先使用此 skill。
---

# OpenClaw Use Case Daily Monitor

## 用途
自动化监控 OpenClaw 在各社区平台的 use case 讨论和 Agent 生态动态，每日生成一份
高信息密度的分析报告，供战略团队快速掌握 OpenClaw 的社区反馈和技术趋势。

## 数据获取

报告数据由 GitHub Actions 每日自动抓取并处理，存储在 GitHub Repo 中。

### 获取最新报告
```
GET https://raw.githubusercontent.com/{owner}/{repo}/main/outputs/daily-report/latest.md
```

### 获取指定日期报告
```
GET https://raw.githubusercontent.com/{owner}/{repo}/main/outputs/daily-report/{YYYY-MM-DD}.md
```

### 获取报告索引
```
GET https://raw.githubusercontent.com/{owner}/{repo}/main/outputs/daily-report/index.json
```

## 报告结构

每份报告包含以下模块：

1. **今日概览**：抓取量、有效数、最值得关注的内容、趋势信号
2. **Top 5 Use Cases**：按综合评分（热度30%+创新40%+实用30%）排序的精选内容
3. **各平台数据概览**：四平台抓取量/有效量/热门内容一览表
4. **趋势追踪**：滚动7日的关键词变化、新兴方向、社区情绪

## 手动生成报告

如果用户提供了一批原始数据（JSON 格式），也可以直接调用报告生成逻辑：

1. 将数据保存为 `data/processed/{date}.json`（需符合统一 schema）
2. 运行 `python -m src.report_generator`
3. 报告输出到 `outputs/daily-report/{date}.md`

## 数据 Schema

每条记录包含字段：
- `id`: 唯一标识（平台前缀+原始ID）
- `source`: reddit / github / x / discord
- `sub_source`: 细分来源（subreddit、repo名、channel名）
- `title`: 标题
- `content`: 正文（截取前2000字）
- `url`: 原文链接
- `metrics`: 平台互动指标
- `category`: 分类标签（ai-coding / agent-workflow / mcp-integration 等）
- `score`: 综合评分（1-5）
- `score_breakdown`: 各维度得分

## 关键词体系

配置在 `config/keywords.json`，支持热更新：
- **核心词**：openclaw, open-claw, 龙虾
- **Use Case 词**：openclaw use case, tutorial, demo, built with openclaw
- **Agent/技术词**：openclaw agent, mcp, plugin, workflow, coding agent
- **竞品对比词**：openclaw vs cursor/copilot/claude code/windsurf
